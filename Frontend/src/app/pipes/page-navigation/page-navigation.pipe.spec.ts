import { PageNavigationPipe } from './page-navigation.pipe';

describe('PageNavigationPipe', () => {
  it('create an instance', () => {
    const pipe = new PageNavigationPipe();
    expect(pipe).toBeTruthy();
  });
});
